version: 2

sources:
  - name: stripe
    database: raw
    tables:
      - name: payment
        description: Raw payment data for jaffle shop
        columns: 
         - name: id
           description: Primary key of payments
           tests:
           - not_null
           - unique